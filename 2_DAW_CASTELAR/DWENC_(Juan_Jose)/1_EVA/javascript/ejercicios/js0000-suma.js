// Escribe un programa que defina una variable suma y que sume a esa variable 0.1 1000 veces.
// Muestra el resultado.

let suma = 0;

for (let i = 0; i < 1000; i++) {
    suma += 0.10000000000000;
    console.log(suma);
} 
